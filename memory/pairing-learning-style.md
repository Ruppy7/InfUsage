# Pairing and learning style

- Follow `Decision → Concept → Build → Checkpoint`.
- The user drives decisions; the AI writes the code.
- Before code, present real alternatives and a recommendation.
- Rust should be annotated for read-fluency the first few times unfamiliar syntax appears.
- Keep explanations pragmatic and connected to the actual code being written.

