# CLAUDE.md — InfUsage

Read `AGENTS.md` and `PLAN.md` first. This project is a pairing/learning project, not just an implementation task.

Current rules:

- Do not make silent technical choices.
- Keep `PLAN.md` decision log current.
- Preserve the provider scope: Codex, Claude/Claude Code, OpenCode Go, and AGY are core; Xiaomi is backlog optional; DeepSeek balance is optional.
- Secrets belong in the trusted host only, never in React state, plugins, logs, or SQLite.

